# Workstreams

Each active or planned workstream should have a file here.

Naming pattern:

```text
<workstream-id>_<short-name>.md
```

The goal is to give any Codex session a one-file summary of:

- ownership
- scope
- dependencies
- changed files
- acceptance target

