#include "SHE/Renderer/NullRendererService.hpp"

// Compatibility source retained so older docs and worktree notes still point at
// a real path. The bootstrap 2D renderer implementation lives in
// Renderer2DService.cpp.
